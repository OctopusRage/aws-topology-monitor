{
  "name": "aws-topology-monitor",
  "version": "0.1.0",
  "private": true,
  "description": "Visualize AWS ELB topology on an n8n-style canvas + node_exporter/CloudWatch monitoring",
  "scripts": {
    "dev": "concurrently -n api,web -c magenta,cyan \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "start": "npm --prefix server start",
    "build": "npm --prefix web run build",
    "setup": "npm --prefix server install && npm --prefix web install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
